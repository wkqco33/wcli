# wcli 개발 계획 (Plan)

이 문서는 Go 언어로 개발 중인 `wcli` (Cobra에서 영감을 받은 경량/고성능 CLI 라이브러리)의 개발 진행 상황과 향후 계획을 정의합니다.

## 개발 철학
- **데이터 중심 & 성능 최우선**: 복잡한 인터페이스 지양, 명확한 `Struct` 사용 및 무거운 서드파티 의존성 최소화.
- **단순하고 직관적인 API**: Cobra의 장점(트리 구조, Flag 바인딩)을 가져오되, 불필요한 추상화 제거.
- **아름다운 터미널 출력**: Python `rich` 패키지에서 영감을 받은 마크업(`[color]text[/color]`) 기반 텍스트 스타일링.

---

## 단계별 구현 계획

### ✅ 1단계: 프로젝트 초기화 및 핵심 기반 구현 (완료)
- `wcli` 모듈 초기화
- 핵심 데이터 구조체 정의 (`Command`, `Context`)
- 단일 명령어 및 하위 명령어(Sub-command) 트리 구성 (AddCommand)
- 루트에서 하위 명령어 탐색 및 실행 라우팅 로직 구현 (`Execute`)
- 명령어 실행 단위 테스트 (`command_test.go`)

### ✅ 2단계: 플래그(Flag) 파싱 시스템 및 Rich 스타일링 구축 (완료)
- 자체 플래그 파싱 엔진 `FlagSet` 구현 (정규식 배제, 성능 최적화)
- 포인터 바인딩 방식의 플래그 선언 지원 (`StringVar`, `IntVar`, `BoolVar`)
- Short (`-f`) 및 Long (`--flag`) 플래그 파싱 로직
- `rich` 서브 패키지 구축: `[red]`, `[bold]` 등의 간단한 마크업 파서 구현
- `rich.Print`, `rich.Println` 유틸리티 함수 구현
- 플래그 파싱 및 마크업 파싱 단위 테스트 완료

### ⏳ 3단계: 도움말(Help) 및 사용법(Usage) 시스템 (예정)
- 명령어의 `Use`, `Short`, `Long` 속성을 기반으로 도움말 텍스트 자동 생성 로직 구현
- 등록된 하위 명령어(`SubCommands`) 및 플래그(`Flags`) 목록 수집 및 포맷팅
- `rich` 패키지를 활용한 컬러풀하고 시인성 높은 도움말 출력
- 커맨드라인에서 `-h`, `--help` 플래그 입력 시 도움말 출력 연결 로직 구현
- 알 수 없는 하위 명령어 또는 플래그 에러 발생 시 자동 도움말 노출 로직 검토

### ⏳ 4단계: 실행 훅(Hook) 및 에러/로깅 구조화 (예정)
- 명령어 실행 전/후에 개입할 수 있는 `PreRun`, `PostRun` 함수 지원
- 체계적인 사용자 정의 에러 타입 정의 및 에러 래핑 (예: `ErrCommandNotFound`)
- 에러 발생 시 `rich` 패키지를 활용한 직관적인 에러 출력 및 핸들링 로직
- *선택 사항: 자동 완성(Autocomplete) 스크립트 생성 기능 기반 연구*
